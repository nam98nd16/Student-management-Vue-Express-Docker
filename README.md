# Student-management-Vue-Express-Docker

# HOW TO RUN

$ docker-compose up


# IMPORTANT
  May need to create an empty folder named "db" (in the root directory containing the other 2 folders (client and server)) before attempting running the above command
